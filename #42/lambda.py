# Functions are objects in python
# we can use number of arguments in lambda but it should be in one expression

f = lambda a, b : a + b     # here f is also a function

result = f(5, 6)

print(result)