from numpy import *

arr = linspace(0, 15)       # if we don't specify parts by default function will create 50 parts
arr1 = linspace(1, 20, 20)  # it is by default float
# linspace(start, stop(included), parts)

print(arr)
print(arr1)